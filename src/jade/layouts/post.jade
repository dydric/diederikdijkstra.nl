include ../includes/mixins.jade

doctype html
html.post.no-js
  include ../includes/head.jade
  body
    .site
      nav.band.band--nav
        p #[a(href="/") Diederik] / {% for category in page.categories %}{{ category }}{% endfor %}
      article.band.band--content
        h1 {{page.title}}
        span.meta #[+showdate("page")] #[+readingtime()]
        | {{ content }}
    include ../includes/foot.jade
